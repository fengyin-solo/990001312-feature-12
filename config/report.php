<?php
/**
 * 举报处置配置
 * 超时升级阈值：举报进入待处理后，超过对应小时数仍未处理则逐级升级
 * 级别 0 普通 -> 1 一级升级 -> 2 二级升级 -> 3 三级升级（最高）
 */

return [
    // 各级别对应的超时阈值（小时）。上报后 deadline = 基准时间 + thresholds[当前级别]
    'escalation_thresholds' => [
        0 => 24,  // 24小时未处理 -> 一级升级
        1 => 24,  // 升级后再过24小时 -> 二级升级
        2 => 48,  // 再过48小时 -> 三级升级
    ],

    // 基准时间：assigned 优先用指派时间，否则用举报时间
    'deadline_base' => 'assigned_or_created',

    // 编辑锁有效期（秒）。锁超时后其他管理员可抢占
    'lock_ttl' => 120,

    // 后台列表轮询间隔（秒），用于多人协同感知状态变化
    'poll_interval' => 10,

    // 页面访问时懒触发升级扫描的最小间隔（秒），避免每次请求都扫表
    'lazy_scan_interval' => 300,
];
